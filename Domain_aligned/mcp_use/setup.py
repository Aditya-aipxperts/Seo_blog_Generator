from setuptools import setup, find_packages

setup(
    name="mcp_use",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # add any required packages here, or leave empty for now
    ],
)
